class Title {
  int x = width/2, y = 200;
  void display() {
    button[0].run();
    button[1].run();
    button[2].run();
    fill(255);
    textAlign(CENTER);
    textSize(75);
    text("cc-remon-game", x, y);
  }
}
